# plex-renamer
A local desktop tool that scans a Plex documentary library, matches video files against TMDB, proposes Plex-compliant renames, and executes approved ones. Built as a React + Vite frontend talking to a Python Flask backend over localhost.
